# TRAFIQO — СЕССИЯ 25.02.2026 FINAL
## Документ для продолжения работы в новом чате

---

## 🏆 ЧТО СДЕЛАЛИ ЗА СЕССИЮ — КРАТКО

| Блок | Тема | Статус |
|------|------|--------|
| 🔐 AUTH | Починили вылет на лендинг при reload | ✅ ЗАКРЫТО |
| 🗄️ DATABASE | Починили 400 ошибку streak_days | ✅ ЗАКРЫТО |
| 🎮 ГЕЙМИФИКАЦИЯ P0 | Единый источник уровней во всём проекте | ✅ ЗАКРЫТО |
| ⚡ EDGE FUNCTION | Формула XP с множителями, деплой на Supabase | ✅ ЗАКРЫТО |

---

## ПРОЕКТ

**Название:** TRAFIQO (MLM Community)
**Репозиторий:** https://mlm-community.vercel.app
**Telegram бот:** @mlm_community_bot
**Стек:** Vanilla JS + Supabase + Vercel
**Лимиты:** JS/TS < 500 строк, CSS < 800 строк, функция < 50 строк

---

## КАК РАБОТАЕМ

1. Я даю промпт → ты отправляешь в VS Code (Cursor / Claude Code)
2. VS Code отвечает → скидываешь мне результат
3. Я объясняю что сделано, для чего и зачем → даю следующий промпт
4. Один файл = один коммит + git push
5. Подтверждение на изменение файла — всегда внутри промпта
6. Перед каждым промптом VS Code читает: CLAUDE.md, MASTER_RULES.md, ARCHITECTURE.md, PROJECT_MAP.md

---

## 🔐 БЛОК 1 — AUTH / RELOAD БАГ

**Проблема:** пользователь в ленте → обновляет страницу → вылетает на лендинг

**Почему было сложно найти:**
Supabase при refresh токена (каждый час) делает сетевой запрос.
Пока запрос идёт — `getSession()` возвращает null.
Два разных места в коде одновременно пытались определить куда роутить → race condition.

**Что починили:**

| Коммит | Файл | Суть фикса |
|--------|------|-----------|
| `52430b3` | auth-core.js | Убрали конкурирующий Путь 1 (authCheckSession), оставили только INITIAL_SESSION как единственный роутер |
| `3bb88d2` | auth-core.js | SIGNED_OUT handler получил guard через sessionStorage — побочный SIGNED_OUT от refresh токена теперь игнорируется, только явный logout кидает на лендинг |

**Supabase URL Configuration (настроено вручную в дашборде):**
- Site URL: `https://mlm-community.vercel.app`
- Redirect URLs: `https://mlm-community.vercel.app` + `http://localhost:3000` + `http://localhost:5173`

> ⚠️ localhost:5173 — это порт Vite (не 3000). Важно что оба добавлены.

---

## 🗄️ БЛОК 2 — DATABASE ФИКС

**Проблема:** `user_stats?select=streak_days` возвращала 400 ошибку

**Причина:** поле `streak_days` находится в таблице `users`, а код запрашивал из `user_stats` где его нет.

| Коммит | Файл | Суть фикса |
|--------|------|-----------|
| (streak) | feed-data.js | `from('user_stats')` → `from('users')`, `.eq('user_id')` → `.eq('id')` |

---

## 🎮 БЛОК 3 — ГЕЙМИФИКАЦИЯ P0 (ЕДИНЫЙ ИСТОЧНИК УРОВНЕЙ)

**Проблема:** 4 разных файла имели свои версии порогов XP — все разные и все неверные.
Пользователь с 10 000 XP в одном месте видел "Конь", в другом "Ферзь".
Расхождение с реальными порогами — в 50 раз.

**Решение:** `js/utils/gamification.js` — единственный источник истины.
Все остальные файлы теперь используют `window.Gamification`.

### Шаг 1 — Довели gamification.js до ТЗ

| Коммит | Что |
|--------|-----|
| `43b8e01` | XP_TABLE 26 записей + STREAK_MULTIPLIERS 9 порогов |

**XP_TABLE пороги (по ТЗ v5.1):**
```
Пешка  ★1-5: 0 / 1000 / 3000 / 7000 / 15000  (временные — подкрутим через админку)
Конь   ★1-5: 25000 / 60000 / 110000 / 180000 / 280000
Слон   ★1-5: 430000 / 630000 / 900000 / 1250000 / 1700000
Ладья  ★1-5: 2300000 / 3100000 / 4000000 / 5000000 / 6000000
Ферзь  ★1-5: 7500000 / 9000000 / 11000000 / 13000000 / 15000000
Король ★1:   25000000
```

**STREAK_MULTIPLIERS (9 порогов по ТЗ):**
```
7д→x1.05 / 21д→x1.1 / 45д→x1.15 / 90д→x1.2 / 120д→x1.3
150д→x1.5 / 180д→x1.7 / 270д→x2.0 / 365д→x2.5
```

### Шаг 2 — Убрали хардкод из 4 файлов

| Коммит | Файл | Старые пороги | После |
|--------|------|---------------|-------|
| `b91d686` | profile.js | 0/500/2000/5000/15000 + локальный getStreakMultiplier | window.Gamification + звёзды |
| `b510b3c` | feed-data.js | 0/500/2000/5000/15000 | window.Gamification |
| `b715b9a` | admin-data.js | 0/500/2000/5000/15000 | window.Gamification |
| `c3207ac` | admin-pages4.js | 0/500/1500/3000/5000/10000 | window.Gamification.XP_TABLE |

---

## ⚡ БЛОК 4 — EDGE FUNCTION complete-task

**Что такое Edge Function:** серверный код на Supabase.
Запускается на сервере — нельзя подделать через DevTools браузера.
XP начисляется только через него.

**Проблема:** функция брала `reward_xp` как есть, без множителей.
`level` и `level_stars` после начисления не обновлялись.

**Формула по ТЗ:**
```
Итоговые XP = Базовые XP × Множитель уровня × Множитель стрика
Пример: 1000 XP × Слон(1.3) × Стрик 21д(1.1) = 1430 XP
```

| Коммит | Файл | Что добавлено |
|--------|------|---------------|
| `2752f59` | complete-task/index.ts | XP_TABLE серверная копия, 3 хелпера (getLevelMultiplier, getStreakMultiplier, calculateLevel), формула XP, пересчёт level/level_stars, расширенный ответ клиенту |

**Ответ клиенту теперь содержит:**
```javascript
{
  success: true,
  xp_awarded: 1430,      // XP с множителями
  xp_total: 26430,       // новый итоговый XP
  level: 'knight',       // новый уровень
  level_stars: 2,        // новые звёзды
  level_up: false,       // повысился ли уровень
  stars_up: true,        // появилась ли новая звезда
  status: 'approved',
  completion_id: '...'
}
```

**Деплой:** выполнен через Supabase CLI
```
supabase login --token <токен из dashboard/account/tokens>
supabase functions deploy complete-task --project-ref tydavmiamwdrfjbcgwny
```
Версия 6, статус ACTIVE, все 15 функций активны.

---

## GAMIFICATION.JS — API

```javascript
window.Gamification = {
  XP_TABLE,            // 26 уровней по ТЗ v5.1
  STREAK_MULTIPLIERS,  // 9 порогов по ТЗ v5.1

  getUserLevel(xpTotal),      // → { level, stars, label, mult }
  getLevelProgress(xpTotal),  // → { percent, current, needed }
  getMultiplier(level),       // → 1.0 / 1.3 / 1.6 / 2.0 / 2.5
  getStreakMultiplier(days),  // → 1.05 → 2.5
  calculateEarnedXP(base, level, streak),
}
```

## XP-ANIMATIONS.JS — API

```javascript
window.showXpToast(xp, label)        // всплывашка "+1.4K XP"
window.showLevelUp(newLevel, color)  // полноэкранный оверлей
window.showStarUnlock(stars, color)  // анимация звезды
```

> Анимации уже подключены. Клиент получает level_up/stars_up
> из complete-task и должен вызывать их — это следующий шаг.

---

## СОСТОЯНИЕ ФАЙЛОВ

| Файл | Строк | Лимит | Статус |
|------|-------|-------|--------|
| js/auth-core.js | 467 | 500 | ⚠️ запас 33 |
| js/utils/gamification.js | 111 | 500 | ✅ |
| js/utils/xp-animations.js | 79 | 500 | ✅ |
| js/screens/profile.js | 288 | 500 | ✅ |
| js/feed-data.js | 420 | 500 | ✅ |
| js/admin-data.js | 399 | 500 | ✅ |
| js/admin-pages4.js | 360 | 500 | ✅ |
| supabase/functions/complete-task/index.ts | 264 | 500 | ✅ |
| css/gamification.css | 208 | 800 | ✅ |
| css/landing.css | 754 | 800 | ⚠️ запас 46 |

---

## ПРИОРИТЕТЫ НА СЛЕДУЮЩУЮ СЕССИЮ

### P1 — ВАЖНО (следующие шаги)

| # | Задача | Файл | Суть |
|---|--------|------|------|
| 1 | 🎴 ДНК-карточка 3 таба | dna-card.js + dna-result.html | Архетип / Сильные стороны / Задания |
| 2 | 🔔 Анимации после задания | tasks.js | Вызов showXpToast/showLevelUp по флагам level_up/stars_up из complete-task |
| 3 | ⭐ Звёзды в profile.html | profile.html | Отобразить level_stars визуально |
| 4 | ⏰ Cron для update-streaks | Supabase Dashboard | Ежедневный автозапуск функции |
| 5 | 🏆 Экран лидерборда | leaderboard.js + template | Топ недели/месяца/всего времени |

### P2 — ПОЗЖЕ

- Streak Recovery (заморозка раз в 60 дней)
- Автоматические ачивки
- Штрафы за невыполнение заданий
- Таймер задания в UI
- PROJECT_MAP.md — обновить (33+ новых файла)

---

## НЕРЕШЁННЫЕ ПРОБЛЕМЫ

| # | Проблема | Приоритет |
|---|----------|-----------|
| 1 | Анимации level_up/stars_up не вызываются после задания | P1 |
| 2 | ДНК-карточка без 3 табов | P1 |
| 3 | Cron update-streaks не настроен | P1 |
| 4 | PROJECT_MAP.md устарел | P2 |

---

## ШАБЛОН ПРОМПТА ДЛЯ СТАРТА НОВОЙ СЕССИИ

```
Ты senior-разработчик проекта TRAFIQO.

Прочитай CLAUDE.md, MASTER_RULES.md, ARCHITECTURE.md, PROJECT_MAP.md.
Подтверди: по 1 правилу из каждого файла.

Контекст предыдущей сессии (25.02.2026 FINAL):

ЗАКРЫТО:
- Reload баг (race condition + SIGNED_OUT guard)
- streak_days 400 ошибка
- Хардкод уровней в 4 файлах → единый window.Gamification
- Edge Function complete-task: формула XP + пересчёт level/stars
  Задеплоена: версия 6, ACTIVE

Последний коммит: 2752f59

Следующий шаг (P1): [ОПИСАНИЕ ЗАДАЧИ]
```

---

*Документ создан: 25.02.2026*
*Последний коммит: 2752f59*
*Edge Function complete-task: версия 6, ACTIVE*
*Ветка: main, синхронизирована с origin*
